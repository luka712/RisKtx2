## Build

### Linux

To build for Linux make sure to copy `scripts/build_linux.template.sh` and crate similary named file such as `scripts/build_linux.sh`
Path for `vcpkg` should be set correctly as requriment.

Then simply run: 
- `./scripts/build_linux.sh release`
- `./scripts/build_linux.sh debug`